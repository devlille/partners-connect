meta {
  name: Approve
  type: http
  seq: 1
}

post {
  url: {{scheme}}://{{base_url}}/events/:event_slug/partnerships/:partnership_id/suggestion-approve
  body: none
  auth: none
}

params:path {
  event_slug: {{event_slug}}
  partnership_id: {{partnership_id}}
}

body:json {
  {
    "pack_id": "{{pack_id}}",
    "option_ids": []
  }
}
