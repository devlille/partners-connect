meta {
  name: Models
  type: http
  seq: 9
}

get {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/ai/models
  body: json
  auth: inherit
}

params:path {
  org_slug: {{org_slug}}
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "prompt": "Reply with: hello"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
