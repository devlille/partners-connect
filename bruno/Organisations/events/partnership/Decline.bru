meta {
  name: Decline
  type: http
  seq: 3
}

post {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/events/:event_slug/partnership/:partnership_id/decline
  body: none
  auth: none
}

params:path {
  event_slug: {{event_slug}}
  org_slug: {{org_slug}}
  partnership_id: {{partnership_id}}
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "pack_id": "{{pack_id}}",
    "option_ids": []
  }
}
