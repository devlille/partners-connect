meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/events/:event_slug/booth-plan
  body: none
  auth: none
}

params:path {
  event_slug: {{event_slug}}
  org_slug: {{org_slug}}
}

headers {
  Authorization: Bearer {{token}}
}
