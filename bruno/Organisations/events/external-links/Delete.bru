meta {
  name: Delete
  type: http
  seq: 2
}

delete {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/events/:event_slug/external-link/:link_id
  body: json
  auth: none
}

params:path {
  link_id: {{link_id}}
  event_slug: {{event_slug}}
  org_slug: {{org_slug}}
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Call for Papers",
    "url": "https://sessionize.com/devlille2025"
  }
}
