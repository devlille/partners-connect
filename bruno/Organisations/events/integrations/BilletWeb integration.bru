meta {
  name: BilletWeb integration
  type: http
  seq: 4
}

post {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/events/:event_slug/integrations/billetweb/ticketing
  body: json
  auth: none
}

params:path {
  event_slug: {{event_slug}}
  org_slug: {{org_slug}}
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "basic": "{{billetweb_basic}}",
    "event_id": "123455",
    "rate_id": "1299342"
  }
}
