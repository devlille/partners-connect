meta {
  name: Update
  type: http
  seq: 3
}

put {
  url: {{scheme}}://{{base_url}}/events/:event_id/partnership/:partnership_id/tickets/:ticket_id
  body: json
  auth: none
}

params:path {
  ticket_id: 
  partnership_id: {{partnership_id}}
  event_id: {{event_id}}
}

body:json {
  {
    "first_name": "John",
    "last_name": "Doe"
  }
}
