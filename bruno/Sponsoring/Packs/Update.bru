meta {
  name: Update
  type: http
  seq: 3
}

put {
  url: {{scheme}}://{{base_url}}/orgs/:org_slug/events/:event_id/packs/:pack_id
  body: json
  auth: none
}

params:path {
  pack_id: {{pack_id}}
  org_slug: {{org_slug}}
  event_id: {{event_id}}
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "name": "Gold",
    "price": 9000,
    "with_booth": true,
    "nb_tickets": 10,
    "max_quantity": 20
  }
}
