{
  "type": "object",
  "title": "SpeakerPartnershipResponse",
  "description": "Response for speaker-partnership attachment",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the speaker-partnership association"
    },
    "speaker_id": {
      "type": "string", 
      "format": "uuid",
      "description": "UUID of the attached speaker"
    },
    "partnership_id": {
      "type": "string",
      "format": "uuid", 
      "description": "UUID of the partnership"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when speaker was attached"
    }
  },
  "required": ["id", "speaker_id", "partnership_id", "created_at"],
  "additionalProperties": false
}