{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Send Partnership Email Request",
  "description": "Request body for sending bulk emails to filtered partnerships",
  "required": ["subject", "body"],
  "properties": {
    "subject": {
      "type": "string",
      "description": "Email subject line (will be prefixed with [event_name])",
      "minLength": 1,
      "maxLength": 500,
      "examples": ["Partnership Update", "Important Event Information"]
    },
    "body": {
      "type": "string",
      "description": "Email body content in HTML format",
      "minLength": 1,
      "examples": ["<h1>Hello Partners</h1><p>Thank you for your support!</p>"]
    }
  },
  "additionalProperties": false
}
